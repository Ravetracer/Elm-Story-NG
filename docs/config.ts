/**
 * Site-wide links. `REPO_URL` is the project's public home. `EDITOR_URL` should
 * point at wherever the browser editor (the `dist-web` build) is deployed — it is
 * the landing page's primary call to action. It defaults to the repository so the
 * button is never dead; set it to the editor's real URL once that is published.
 */
export const REPO_URL = 'https://github.com/Ravetracer/Elm-Story-NG'

// The deployed browser editor. This site is the landing at
// elm-story-ng.ravetracer.de; the editor lives on its own domain.
export const EDITOR_URL = 'https://elm-story-ng-edit.ravetracer.de'

export const LICENSE = 'GPL-3.0-or-later'
